name: Legacy reference normalization (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled. Reference normalization is maintained in app.py."
